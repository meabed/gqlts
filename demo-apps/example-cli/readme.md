# Gqlts CLI Example

This package is a small CLI playground for generating a client from a remote GraphQL endpoint with the local `cli/src/cli.ts` entrypoint.

## Commands

```sh
bun run examplecli
bun run runexample
```

Use this app for quick manual CLI checks. Use [../../docs/testing.md](../../docs/testing.md) for the maintained test matrix.
